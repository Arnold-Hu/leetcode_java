public class P149 {
    public int maxPoints(int[][] points) {
        if (points.length == 0) {
            return 0;
        }
        if (points.length == 1) {
            return 1;
        }

        if (points.length == 2) {
            return 2;
        }

        for (int i=0; i<points.length - 2; i++) {
            for (int j)
        }

    }
}
